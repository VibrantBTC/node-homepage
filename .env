# =============================
# General app
# =============================
# The title shown at the top of the page
APP_TITLE=Bitcoin Node

# The port that the Flask app listens on inside the container
APP_PORT=8088

# =============================
# Bitcoin Core RPC (for stats)
# =============================
# Host and port for Bitcoin Core RPC. If you use network_mode: host in Docker,
# 127.0.0.1 will point to the host. Otherwise, use the LAN IP of your node.
BITCOIN_RPC_HOST=127.0.0.1
BITCOIN_RPC_PORT=8332

# RPC username/password from your bitcoin.conf (or service). Required for stats.
BITCOIN_RPC_USER=bitcoin
BITCOIN_RPC_PASS=bitcoin

# =============================
# Bitcoin P2P (display only)
# =============================
# Tor onion for your node’s P2P (for wallets), and the port (usually 8333).
# The app shows this and generates a QR. Click to copy host or port individually.
BITCOIN_P2P_ONION=yourbitcoinp2paddress.onion
BITCOIN_P2P_PORT=8333

# =============================
# Fulcrum (Electrum server)
# =============================
# These are DISPLAY/QR endpoints for your users. They do not affect stats.
# local_address is your LAN hostname (e.g., nodebox.local). The onion addresses
# are your Tor hidden services. TCP and SSL ports are usually 50001 and 50002.
FULCRUM_TCP_PORT=50001
FULCRUM_SSL_PORT=50002
FULCRUM_LOCAL_ADDRESS=hostname.local
FULCRUM_ONION_TCP=fulcrumtcpaddress.onion
FULCRUM_ONION_SSL=fulcrumssladdress.onion

# Show this in the Fulcrum header (logs don't include version).
FULCRUM_VERSION=2.0.0

# =============================
# Fulcrum stats (logs-only)
# =============================
# If true: show Fulcrum sync progress from a tiny tail file the host writes
# with cron. If false: hide the Fulcrum sync bar/percent/speeds entirely
# (endpoints/QRs still shown).
FULCRUM_STATS=true

# Path INSIDE the container where the tail file is mounted. docker-compose.yml
# maps ./fulcrum_tail.log (host) -> /app/fulcrum_tail.log (container).
# Suggested to leave this at its default
FULCRUM_TAIL_PATH=/app/fulcrum_tail.log

# How many lines from the tail file to scan for "Processed height..." and
# "Block height ..., up-to-date". 200 lines is small, fast, and enough.
FULCRUM_TAIL_LINES=200

# =============================
# Dojo pairing JSON
# =============================
# Fill these fields and the app will build the JSON for you.
# The final JSON's explorer.url will be overridden to use MEMPOOL_ONION if set.
DOJO_APIKEY=
DOJO_URL=
DOJO_VERSION=1.27.0
EXPLORER_URL=

# =============================
# Dojo Maintenance Tool Url
# =============================
# Enter the Onion URL for your DMT so You have quick access by clicking the "Samourai Dojo" Heading
DOJO_MAINTENANCE_URL=DMT.onion

# =============================
# Mempool and Robosats
# =============================
# The card titles are clickable clearnet links; Tor cards/QRs are full width.
MEMPOOL_CLEARNET=https://mempool.example.com
MEMPOOL_ONION=mempoolonionaddress.onion

ROBOSATS_CLEARNET=https://robosats.example.com
ROBOSATS_ONION=robosatsonionaddress.onion

# =============================
# Monero (display only)
# =============================
# The app shows an Onion:Port and a QR for convenience. No RPC stats are pulled.
MONERO_ONION=moneroonionaddress.onion
MONERO_RPC_PORT=18089
